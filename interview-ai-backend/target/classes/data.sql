-- InterviewAI 题库种子数据（50道真实面试题）
-- 覆盖：Java基础、Spring、MySQL、Redis、并发、JVM、微服务、网络、操作系统、设计模式

INSERT IGNORE INTO question (title, content, answer, tags, difficulty) VALUES
-- ========== Java基础 (8题) ==========
('String、StringBuilder和StringBuffer的区别', '请从可变性、线程安全、性能三个维度对比这三者。', 'String是不可变对象，每次修改都会创建新对象，适合少量字符串操作。StringBuilder可变，非线程安全，单线程下性能最高。StringBuffer可变，方法用synchronized修饰，线程安全但性能略低于StringBuilder。字符串拼接场景优先用StringBuilder，多线程共享变量用StringBuffer。', '["Java","基础","字符串"]', 1),
('==和equals的区别', '请详细说明两者的比较规则，以及hashCode和equals的关系。', '==比较的是栈中的值：基本类型比较值，引用类型比较堆内存地址。equals默认是Object的方法，也是比较地址，但String、Integer等重写了equals，改为比较内容。重写equals必须重写hashCode：两个对象equals相等则hashCode必须相等，hashCode相等但equals不一定相等（哈希碰撞）。这是HashMap等集合正确工作的前提。', '["Java","基础"]', 1),
('Java中的异常体系', '请描述Throwable、Error、Exception、RuntimeException之间的关系和各自的使用场景。', 'Throwable是所有异常和错误的父类。Error表示严重的JVM级问题（如OutOfMemoryError），程序不应捕获。Exception分两类：受检异常（如IOException）必须显式catch或throws；非受检异常即RuntimeException（如NullPointerException）不需要强制处理。业务异常应继承RuntimeException，避免方法签名污染。', '["Java","基础","异常"]', 1),
('ArrayList和LinkedList的区别及使用场景', '请从底层数据结构、增删改查性能、内存占用等方面对比。', 'ArrayList底层是Object[]数组，支持O(1)随机访问，尾部插入O(1)，中间插入/删除O(n)（需要移动元素）。LinkedList底层是双向链表，随机访问O(n)，头尾插入O(1)，中间插入（已定位到节点）O(1)。ArrayList内存连续，缓存友好；LinkedList节点额外存储前后指针，内存开销大。频繁随机访问用ArrayList，频繁头尾插入用LinkedList（或ArrayDeque更优）。', '["Java","集合","数据结构"]', 1),
('Java中final关键字的用法', '请列举final可以修饰什么，分别产生什么效果。', 'final可修饰三类目标：1.修饰类——类不可被继承，如String、Integer。2.修饰方法——方法不可被子类重写。3.修饰变量——基本类型值不可变，引用类型地址不可变但对象内容可修改。final修饰的成员变量必须在定义时、构造块中或构造方法中赋值。配合static final定义常量。面试加分：final修饰形参时方法内不可重新赋值。', '["Java","基础","关键字"]', 1),
('深拷贝和浅拷贝的区别', '请说明两者的实现方式和各自的适用场景。', '浅拷贝：只复制对象本身，对象内部的引用类型字段仍指向同一个对象（两个对象的字段共享同一块内存）。实现方式：Object.clone()默认是浅拷贝，或手动逐字段赋值。深拷贝：对象本身及所有引用类型字段都递归复制，两个对象完全独立。实现方式：手动递归clone、序列化/反序列化（如JSON序列化）、或使用Apache Commons的SerializationUtils。', '["Java","基础"]', 2),
('Java 8的Stream API有哪些常用操作', '请列举中间操作和终端操作，并说明惰性求值的原理。', 'Stream操作分为两类：1.中间操作（惰性求值）：filter（过滤）、map（转换）、flatMap（扁平化）、distinct（去重）、sorted（排序）、peek（调试）、limit/skip（截取）。2.终端操作（触发计算）：collect（收集）、forEach（遍历）、reduce（归约）、count（计数）、anyMatch/allMatch/noneMatch（匹配）、findFirst/findAny（查找）。惰性求值指中间操作不会立即执行，只有终端操作触发时，整个流水线才按需执行，避免中间结果的内存浪费。', '["Java","Java8","Stream"]', 2),
('Java反射机制的原理及应用场景', '请说明如何获取Class对象，反射能做什么，以及它的性能影响。', '获取Class对象三种方式：Class.forName("全类名")、对象.getClass()、类名.class。反射可以：运行时获取类的构造方法/字段/方法并调用、突破private访问限制、动态代理等。应用场景：Spring IoC容器（通过反射实例化Bean并注入依赖）、JDBC驱动加载（Class.forName）、ORM框架映射。性能影响：反射调用比直接调用慢（方法调用需要额外检查、装箱拆箱），JIT优化效果差。setAccessible(true)可提升反射调用性能。', '["Java","反射","框架"]', 2),

-- ========== Spring (6题) ==========
('Spring中Bean的作用域', '请列举并解释Spring支持的几种Bean作用域。', '1.singleton（默认）：整个IoC容器中只有一个Bean实例。2.prototype：每次注入或getBean()都创建新实例。3.request：每个HTTP请求创建一个实例（Web环境）。4.session：每个HTTP Session创建一个实例（Web环境）。5.application：每个ServletContext生命周期一个实例。6.websocket：每个WebSocket生命周期一个实例。开发中绝大多数场景用singleton，需要状态隔离时用prototype。注意：prototype Bean注入到singleton Bean时不会每次创建新实例，需要用@Lookup或ObjectFactory解决。', '["Spring","IoC"]', 2),
('Spring事务的传播行为', '请介绍常见的几种传播行为及其含义。', 'PROPAGATION_REQUIRED（默认）：当前有事务则加入，无则新建。PROPAGATION_REQUIRES_NEW：总是新建事务，当前事务挂起。PROPAGATION_SUPPORTS：当前有事务则加入，无则以非事务方式运行。PROPAGATION_NOT_SUPPORTED：以非事务方式运行，当前事务挂起。PROPAGATION_MANDATORY：必须在事务中运行，否则抛异常。PROPAGATION_NEVER：不得在事务中运行，否则抛异常。PROPAGATION_NESTED：嵌套事务，可部分回滚。实际项目中REQUIRED覆盖80%场景，REQUIRES_NEW用于记录日志（独立于主事务提交）。', '["Spring","事务"]', 2),
('@Autowired和@Resource的区别', '请从注解来源、注入策略、属性支持等角度对比。', '@Autowired是Spring注解，默认按类型（byType）注入，配合@Qualifier可指定Bean名称。required属性可设为false，找不到Bean时为null。@Resource是JDK注解（javax.annotation），默认按名称（byName）注入，找不到名称再按类型匹配，无法设置是否必须。@Autowired配合@Primary可解决多个同类型Bean的歧义。面试建议：项目中统一用一种即可，团队通常选@Autowired。', '["Spring","IoC","注解"]', 1),
('Spring AOP的实现原理', '请说明AOP的两种代理方式及其区别。', 'Spring AOP基于动态代理：1.JDK动态代理——目标类实现了接口时使用，通过java.lang.reflect.Proxy生成代理对象，代理类和目标类实现相同接口。2.CGLIB代理——目标类没有接口时使用，通过继承目标类生成子类，重写父类方法实现增强，final方法无法代理。Spring Boot 2.x开始默认使用CGLIB。AOP核心概念：切面(Aspect)、切点(Pointcut)、通知(Advice)：前置/后置/环绕/异常/最终通知。实际应用：事务管理(@Transactional)、日志记录、权限校验。', '["Spring","AOP","原理"]', 2),
('Spring Bean的生命周期', '请描述从创建到销毁的完整流程。', '1.实例化：通过反射调用构造方法创建对象。2.属性赋值：包括@Autowired注入和@Value属性赋值。3.BeanNameAware.setBeanName()。4.BeanFactoryAware.setBeanFactory()。5.ApplicationContextAware.setApplicationContext()。6.BeanPostProcessor.postProcessBeforeInitialization()。7.@PostConstruct标注的方法。8.InitializingBean.afterPropertiesSet()。9.自定义init-method。10.BeanPostProcessor.postProcessAfterInitialization()。11.Bean就绪可用。12.容器关闭时：@PreDestroy、DisposableBean.destroy()、自定义destroy-method依次执行。', '["Spring","IoC","生命周期"]', 3),
('Spring MVC处理请求的完整流程', '请从前端请求到后端响应的全过程进行描述。', '1.请求到达DispatcherServlet（前端控制器）。2.DispatcherServlet调用HandlerMapping查找处理器（根据URL匹配Controller方法）。3.HandlerMapping返回HandlerExecutionChain（包含HandlerInterceptor链）。4.DispatcherServlet调用HandlerAdapter执行处理器。5.HandlerAdapter调用具体的Controller方法。6.Controller处理业务逻辑后返回ModelAndView（或@ResponseBody直接写响应）。7.DispatcherServlet调用ViewResolver解析视图。8.视图渲染Model数据生成HTML响应。9.依次执行拦截器的afterCompletion方法。对于前后端分离的REST API项目，Controller直接返回JSON，跳过ViewResolver。', '["Spring","SpringMVC","原理"]', 2),

-- ========== MySQL (7题) ==========
('MySQL中InnoDB和MyISAM的区别', '请从事务支持、锁粒度、外键、崩溃恢复等角度对比。', 'InnoDB：支持事务(ACID)、行级锁、支持外键、支持崩溃恢复（redo log+undo log）、聚簇索引存储（主键和数据存在一起）。MyISAM：不支持事务、表级锁、不支持外键、崩溃后恢复困难、索引和数据分开存储、支持全文索引。InnoDB是MySQL 5.5后的默认引擎，生产环境首选。MyISAM优势：简单查询快、磁盘占用小，适用于读多写少且不需要事务的日志/统计场景。', '["MySQL","存储引擎"]', 1),
('MySQL索引的数据结构为什么选择B+树', '请对比B树、B+树、红黑树、哈希表，说明选择B+树的原因。', 'B+树相比B树：1.非叶子节点不存数据只存索引，单个节点能存更多索引键，树更矮，减少磁盘IO。2.叶子节点通过双向链表连接，范围查询效率高。相比红黑树：B+树是多路搜索树，树高更低，适合磁盘IO场景（一次IO读一个节点）。相比哈希表：哈希表O(1)只支持等值查询，不支持范围查询和排序。B+树天然适合数据库的等值查询、范围查询、排序和分页。', '["MySQL","索引","数据结构"]', 2),
('什么是索引覆盖', '请说明覆盖索引的概念以及它如何提升查询性能。', '覆盖索引是指查询的所有列都在索引中，不需要回表查数据行。例如联合索引(a,b,c)，查询SELECT a,b,c FROM t WHERE a=1，所有列都被索引覆盖。性能提升：不需要回表查聚簇索引，减少磁盘IO。EXPLAIN中Extra列显示"Using index"表示使用了覆盖索引。优化实践：SELECT只查必要列，配合联合索引设计实现覆盖查询，避免SELECT *导致无法使用覆盖索引。', '["MySQL","索引","优化"]', 2),
('MySQL的隔离级别及解决的问题', '请列举四种隔离级别，并说明它们能解决及不能解决的并发问题。', '1.READ UNCOMMITTED（读未提交）：解决丢失更新，但存在脏读、不可重复读、幻读。2.READ COMMITTED（读已提交，Oracle默认）：解决脏读，存在不可重复读和幻读。3.REPEATABLE READ（可重复读，InnoDB默认）：解决脏读和不可重复读，InnoDB通过间隙锁+MVCC也解决了大部分幻读。4.SERIALIZABLE（串行化）：全部解决，但并发度极低。InnoDB的MVCC在RR级别下：事务开始时创建Read View，SELECT读到快照数据（一致性读），UPDATE/DELETE使用当前读+间隙锁防止幻读。', '["MySQL","事务","隔离级别"]', 2),
('MySQL慢查询如何排查和优化', '请描述从发现问题到解决问题的完整流程。', '1.开启慢查询日志：slow_query_log=ON，long_query_time设定阈值（通常1秒）。2.分析慢查询：mysqldumpslow工具统计高频慢查询，或使用Percona Toolkit的pt-query-digest。3.EXPLAIN分析执行计划：关注type列（ALL最差，index/range/ref/eq_ref/const依次更优）、rows列（扫描行数）、Extra列（Using filesort/Using temporary需优化）。4.优化手段：加索引（优先where条件和order by列）、改写SQL（避免SELECT *、避免函数包裹索引列）、分库分表、读写分离。', '["MySQL","优化","慢查询"]', 3),
('什么是回表查询，如何避免', '请解释回表的概念，并给出优化方案。', '回表是指使用二级索引（非聚簇索引）查到主键ID后，还需要回聚簇索引取出完整行数据。例如索引为(name)，查询SELECT * FROM t WHERE name='张三'，先从name索引中找到主键id，再回聚簇索引查完整行。避免方案：1.覆盖索引——只查索引包含的列。2.适当冗余列建立联合索引。3.EXPLAIN中extra显示"Using index"表示未回表。注意：并非所有回表都要优化，只有当回表次数巨大且导致性能问题时才需要。', '["MySQL","索引","优化"]', 2),
('分库分表方案及常见问题', '请介绍常见的分库分表策略和带来的挑战。', '分库分表策略：1.垂直分库：按业务模块（用户库、订单库、商品库）。2.垂直分表：按列拆分（主表存常用字段，扩展表存大字段）。3.水平分库：按用户ID哈希分库。4.水平分表：单表数据量过大时按ID取模分布。带来的问题：跨库JOIN无法使用（需应用层或冗余字段解决）、分布式事务（Seata/RocketMQ事务消息）、全局唯一ID（雪花算法/美团Leaf）、分页查询困难（需全局排序后分页）。中小项目优先单库优化+缓存+读写分离，谨慎引入分库分表。', '["MySQL","分库分表","架构"]', 3),

-- ========== Redis (6题) ==========
('Redis的5种基本数据类型及应用场景', '请列举并说明每种类型的数据结构和典型用途。', '1.String：简单K-V，可存数字（计数器、分布式锁的值、验证码）、JSON（缓存对象）。2.List：双向链表，用作消息队列（LPUSH+RPOP或BRPOP阻塞消费）、最新列表（LPUSH+LTRIM保留最近N条）。3.Hash：存对象字段（如用户信息各字段独立存取），避免整体序列化开销。4.Set：无序不重复集合，好友关系的交集/并集/差集、抽奖去重。5.ZSet：有序集合，排行榜（score为分数）、延迟队列（score为执行时间戳）、带权重的标签系统。', '["Redis","数据类型"]', 1),
('Redis过期删除策略和内存淘汰策略', '请说明Redis如何清理过期key以及在内存满时的处理方式。', '过期删除策略：1.惰性删除——访问key时检查是否过期，过期则删除。2.定期删除——每100ms随机抽取一批key检查并删除过期的。两者结合，兼顾CPU和内存。内存淘汰策略（maxmemory-policy）：1.noeviction——内存满后拒绝写入。2.allkeys-lru——所有key中淘汰最近最少使用的。3.volatile-lru——有过期时间的key中淘汰。4.allkeys-lfu——淘汰使用频率最低的。5.volatile-ttl——淘汰即将过期的。6.allkeys-random——随机淘汰。生产环境常用allkeys-lru。', '["Redis","内存","淘汰策略"]', 2),
('Redis持久化RDB和AOF的区别', '请从原理、优缺点、使用场景等方面对比。', 'RDB（快照）：按时间间隔将内存数据写入磁盘快照（fork子进程写临时文件后替换）。优点：文件紧凑、恢复快、对性能影响小。缺点：两次快照间宕机会丢失数据。AOF（追加日志）：每条写命令追加到日志文件。fsync策略：always（每条同步，最安全但慢）、everysec（每秒钟同步，生产推荐）、no（操作系统决定）。优点：数据安全性高、日志可读可修复。缺点：文件大、恢复慢。生产环境建议RDB+AOF混合使用：Redis 4.0后支持混合持久化，AOF先以RDB格式写快照，再追加增量命令，兼顾恢复速度和数据安全。', '["Redis","持久化","RDB","AOF"]', 2),
('Redis哨兵模式和集群模式的区别', '请从架构、故障转移、数据分布、扩容等方面对比。', '哨兵模式（Sentinel）：解决高可用。由多个Sentinel节点监控主从节点，主节点宕机时自动选举新主节点并切换。但所有数据仍在一个主节点上，受单机内存限制。集群模式（Cluster）：解决高可用+数据分布。数据按16384个slot分布到多个主节点（每个节点管理一部分slot），每个主节点可有自己的从节点。客户端根据CRC16(key)%16384路由到对应节点。扩展性：集群模式可通过增加节点并重新分配slot实现水平扩容。哨兵模式适用于数据量可控（<10GB）且对高可用有要求的场景；集群模式适用于大数据量+高并发场景。', '["Redis","高可用","哨兵","集群"]', 3),
('Redis的单线程模型为什么这么快', '请从多路复用、内存操作、数据结构等方面解释。', '1.Redis基于Reactor模式的事件驱动：使用epoll/kqueue等多路复用技术，一个线程监听多个客户端连接，有事件才处理，避免了多线程上下文切换和锁竞争。2.纯内存操作：所有数据在内存中，读写速度是纳秒级。3.高效数据结构：SDS（简单动态字符串）代替C字符串、压缩列表、跳表等优化数据结构，减少内存碎片和时间复杂度。4.避免CPU成为瓶颈的单点：Redis 6.0后引入多线程I/O（网络读写用多线程），核心命令执行仍单线程保证原子性。实际瓶颈多在网络带宽和内存大小，而非CPU。', '["Redis","单线程","性能"]', 2),
('Redis实现分布式锁的方案及RedLock算法', '请说明使用Redis实现分布式锁的要点，以及RedLock算法的原理。', '基础实现：SET lock_key unique_value NX EX 30。unique_value确保只有锁持有者能释放（释放时用Lua脚本比较value再删除，防止误删别人的锁）。EX设置过期时间防止死锁。问题：主从切换时锁可能丢失（锁在主节点上还没同步到从节点就故障切换了）。RedLock算法：1.向N个Redis节点（N为奇数，通常5个）独立获取锁。2.如果在超过半数(N/2+1)节点上获取锁且总耗时小于锁的有效时间，则获取成功。3.锁的有效时间=初始设置时间-获取耗时-时钟漂移补偿。RedLock争议较大，Martin Kleppmann和Redis作者Antirez有过论战。生产环境建议用Redisson的RLock（看门狗自动续期+可重入锁），对一致性要求极高时考虑ZooKeeper。', '["Redis","分布式锁","RedLock"]', 3),

-- ========== 并发编程 (5题) ==========
('Java中创建线程的几种方式', '请列举并评价各自优劣。', '1.继承Thread类：重写run方法，Java单继承限制扩展性，不推荐。2.实现Runnable接口：实现run方法，可复用任务逻辑，无返回值，不能抛受检异常。3.实现Callable接口+FutureTask：call方法有返回值且可抛异常，配合Future.get()获取结果（阻塞）。以上三种底层都是通过Thread.start()启动线程。实际项目中直接创建线程不推荐（资源不可控），应使用线程池（ExecutorService）管理线程生命周期。', '["Java","并发","线程"]', 1),
('线程池的核心参数及工作流程', '请说明ThreadPoolExecutor的7个参数和任务提交后的处理流程。', '7个参数：corePoolSize（核心线程数）、maximumPoolSize（最大线程数）、keepAliveTime（空闲线程存活时间）、unit（时间单位）、workQueue（阻塞队列）、threadFactory（线程工厂）、handler（拒绝策略）。工作流程：1.任务提交后，先判断核心线程是否满，不满则创建核心线程执行。2.核心线程满则任务放入workQueue等待。3.队列满且线程数<maximumPoolSize则创建非核心线程执行。4.队列满且线程数=maximumPoolSize则执行拒绝策略。四种拒绝策略：AbortPolicy（抛异常，默认）、CallerRunsPolicy（回退给调用者线程执行）、DiscardPolicy（静默丢弃）、DiscardOldestPolicy（丢弃最旧任务）。', '["Java","并发","线程池"]', 2),
('volatile关键字的作用和原理', '请说明volatile能保证什么、不能保证什么，以及底层如何实现。', 'volatile两大作用：1.可见性——一个线程修改volatile变量后，其他线程立即可见（强制从主内存读取，修改后立即写回主内存，通过CPU缓存一致性协议MESI实现）。2.禁止指令重排序——volatile变量前后的指令不会被JVM重排序（通过内存屏障实现：写volatile前插入StoreStore屏障，写后插入StoreLoad屏障；读volatile后插入LoadLoad和LoadStore屏障）。不能保证：原子性（volatile i++不是原子操作，多线程下不安全）。典型应用：DCL单例模式中instance用volatile修饰（防止指令重排导致未初始化的对象被返回）、状态标志位（如shutdown标志）。', '["Java","并发","volatile","JMM"]', 2),
('ThreadLocal的原理及内存泄漏问题', '请说明ThreadLocal的实现原理和为什么可能导致内存泄漏，如何预防。', '原理：每个Thread内部有一个ThreadLocalMap（实质是Entry[]数组，Entry的key是ThreadLocal的弱引用，value是存储值）。调用ThreadLocal.get()时，先获取当前线程的ThreadLocalMap，以ThreadLocal对象为key查找对应value。内存泄漏原因：ThreadLocal被GC回收（弱引用），但Entry中的value是强引用且线程存活，value无法被回收。如果线程是线程池中的线程（长期存活），累积的value会撑爆内存。预防措施：使用完ThreadLocal后必须调用remove()方法清除数据。Spring的@Transactional事务管理中就用了ThreadLocal存储数据库连接。', '["Java","并发","ThreadLocal"]', 2),
('AQS（AbstractQueuedSynchronizer）的原理', '请简要说明AQS的实现思想和它在JUC中的应用。', 'AQS是JUC的核心框架，基于FIFO双向队列和int state变量实现。核心思想：1.用state表示同步状态（如ReentrantLock中0表示未锁定，>0表示重入次数）。2.获取锁失败的线程被包装成Node加入等待队列尾部并park阻塞。3.释放锁的线程唤醒队列头部节点的后继节点。AQS提供了共享模式（Semaphore、CountDownLatch）和独占模式（ReentrantLock）。主要方法：tryAcquire/tryRelease（独占）、tryAcquireShared/tryReleaseShared（共享），由子类实现具体逻辑。面试必问：AQS是理解JUC的钥匙。', '["Java","并发","AQS","JUC"]', 3),

-- ========== JVM (5题) ==========
('JVM内存模型（运行时数据区）', '请画出JVM的内存结构，说明各区域的作用和可能抛出的异常。', '1.程序计数器：当前线程执行字节码的行号，线程私有，唯一不抛OOM的区域。2.Java虚拟机栈：方法的栈帧（局部变量表、操作数栈、动态链接、返回地址），线程私有，StackOverflowError（递归过深）或OutOfMemoryError。3.本地方法栈：Native方法。4.堆：对象实例存储区，线程共享，GC主要区域，OOM。5.方法区（元空间）：类信息、常量、静态变量、JIT编译后代码缓存，线程共享。JDK8后方法区由Metaspace实现，使用本地内存替代永久代，默认无上限(-XX:MaxMetaspaceSize限制)。运行时常量池在Metaspace中。', '["JVM","内存模型"]', 2),
('JVM类加载机制和双亲委派模型', '请描述类加载的生命周期，以及双亲委派模型的工作原理和打破方式。', '类加载生命周期：加载→验证→准备→解析→初始化→使用→卸载。双亲委派模型：类加载器收到加载请求后，先委派给父加载器（Application→Extension→Bootstrap），父加载器能加载则返回，不能则子加载器自己加载。优点：避免核心类被篡改（如自定义java.lang.String会被Bootstrap拦截）、保证Java类库的类型安全。打破双亲委派：1.重写loadClass()（如Tomcat为隔离不同Web应用的类）。2.JDBC的DriverManager通过线程上下文类加载器打破（SPI机制）。3.OSGi模块化框架的网状类加载器。', '["JVM","类加载","双亲委派"]', 2),
('JVM垃圾回收器对比：CMS和G1', '请从算法、停顿时间、适用场景等方面对比CMS和G1。', 'CMS（Concurrent Mark Sweep）：老年代回收器，标记-清除算法，追求最短停顿。四阶段：初始标记（STW短）、并发标记、重新标记（STW短）、并发清除。缺点：产生内存碎片（需要Serial Old兜底）、浮动垃圾、对CPU敏感。G1（Garbage First）：JDK9默认，采用Region分区+标记-整理算法，可设定停顿目标(-XX:MaxGCPauseMillis)。将堆分为大小相等的Region，优先回收垃圾最多的Region。支持混合回收（同时回收年轻代和部分老年代）。G1内存占用更大（需要Remember Set记录跨Region引用）。选择建议：JDK8及以下、堆<4G用CMS或Parallel；JDK9+、大堆（>4G）用G1；超低延迟（<10ms）用ZGC或Shenandoah。', '["JVM","GC","CMS","G1"]', 3),
('JVM调优的思路和常用参数', '请分享你在项目中遇到过的JVM问题及排查调优经验。', '调优思路：1.明确目标——减少停顿时间还是提高吞吐量。2.排查问题——查看GC日志(-Xlog:gc:*)，jstat查看GC频率和时长，jmap查看内存分布，jstack查看线程状态。3.调整参数——根据GC日志分析年轻代/老年代比例是否合理。常用参数：-Xms/-Xmx（堆初始/最大，生产建议二者相等防扩展）、-Xmn（年轻代大小）、-XX:MetaspaceSize/-XX:MaxMetaspaceSize、-XX:+UseG1GC（指定收集器）、-XX:MaxGCPauseMillis（G1停顿目标）、-XX:+HeapDumpOnOutOfMemoryError（OOM时dump堆快照）。调优不是调越大越好，应在理解应用行为的基础上精确调整。', '["JVM","调优","GC"]', 3),
('什么是Stop The World，为什么会发生', '请说明STW的概念、触发场景及减少STW的方案。', 'STW（Stop The World）指JVM在执行GC时暂停所有应用线程的现象。原因是GC需要定位根对象（GC Roots）：如果应用线程不停下来，引用关系变化会导致根对象不一致。触发场景：Minor GC（年轻代GC）和Full GC都会引发STW。CMS和G1尽量减少STW时长（采用并发标记），但无法完全避免（初始标记和重新标记阶段仍需STW）。减少STW方案：1.减少Full GC次数（优化代码减少对象创建、调大年轻代）。2.使用低延迟GC（G1设MaxGCPauseMillis，极端场景用ZGC）。3.减少GC Roots数量（避免过多静态引用和线程栈引用）。', '["JVM","GC","STW"]', 2),

-- ========== 微服务 (4题) ==========
('微服务架构的优缺点', '请从架构设计的角度分析微服务和单体架构的取舍。', '优点：1.独立部署和扩展（各服务独立扩缩）。2.技术异构（不同服务可用不同语言/框架）。3.故障隔离（一个服务宕机不影响全局）。4.团队自治（小团队端到端负责服务）。缺点：1.分布式复杂性（网络延迟、数据一致性、服务间调用链追踪）。2.运维复杂度（监控、日志、CI/CD链路过长）。3.数据一致性挑战（分布式事务、最终一致性）。4.接口管理成本（服务间契约管理）。选择原则：项目初期和中小团队优先单体架构，业务明确且团队>20人再考虑拆分。', '["微服务","架构"]', 2),
('服务注册与发现的原理', '请介绍常见的注册中心及其工作原理。', '三要素：注册中心、服务提供者、服务消费者。流程：1.服务提供者启动时向注册中心注册（IP+端口+元数据）。2.服务消费者从注册中心拉取服务列表或订阅变更通知。3.消费者通过负载均衡选择实例调用。4.提供者定时发送心跳续约，超时未续约则注册中心将其剔除。5.提供者下线时主动注销。常见注册中心：Nacos（AP+CP模式可选，与Spring Cloud Alibaba集成最好）、Consul（CP，自带健康检查）、Eureka（AP，2.x已停止维护）。CAP权衡：Eureka/Nacos默认AP保可用性，Consul/ZooKeeper选CP保一致性。', '["微服务","注册中心","Nacos"]', 2),
('分布式事务的解决方案', '请介绍几种常见的分布式事务方案及各自的适用场景。', '1.两阶段提交(2PC)：协调者→参与者prepare→全部成功才commit，否则全部rollback。强一致性但阻塞时间长，Seata AT模式基于2PC改良。2.TCC(Try-Confirm-Cancel)：Try预留资源→Confirm全部确认→任何一个失败则Cancel回滚。对业务侵入大，但灵活性高，适用于资金交易。3.可靠消息最终一致性：本地事务+消息表+RocketMQ事务消息。先发半消息→执行本地事务→根据结果提交或回滚消息→消费者消费消息执行本地操作并ACK。4.最大努力通知：不断重试直到成功（如支付结果通知），配合定时任务补偿。5.Saga模式：长事务拆为多个本地事务，每个事务有对应的补偿操作，失败时逆序补偿。适用于流程长的业务场景。', '["微服务","分布式事务","Seata"]', 3),
('服务熔断、降级和限流的区别', '请说明三者的概念和各自的实现方式。', '熔断（断路器模式）：下游服务异常时快速失败，不再继续调用该服务，防止级联故障。状态机：关闭→（失败次数达阈值）→打开→（等待时间到）→半开→（试探成功）→关闭。降级：系统压力大或有故障时，关闭边缘功能或有损服务，保证核心链路可用。如电商大促时关闭推荐功能，核心下单链路有损降级为排队处理。限流：控制请求速率（QPS限流、并发线程数限流），防止系统过载。实现：Sentinel整合Sentinel Dashboard做流量控制；Resilience4j轻量级熔断降级库；Guava RateLimiter单机限流。Sentinel功能最全面（熔断+降级+限流一体），是阿里开源的主流方案。', '["微服务","熔断","降级","限流"]', 3),

-- ========== 计算机网络 (4题) ==========
('HTTP和HTTPS的区别', '请从协议层级、安全性、连接方式等方面对比，并说明HTTPS的握手过程。', 'HTTP明文传输，HTTPS=HTTP+TLS/SSL加密。主要区别：1.HTTPS需要CA证书验证服务器身份。2.HTTPS通过TLS握手协商对称加密密钥（非对称加密交换密钥+对称加密传输数据）。3.HTTPS默认端口443，HTTP是80。4.HTTPS可防止中间人攻击和篡改。TLS握手（简化）：客户端发送支持的加密套件+随机数→服务端选加密套件+证书+随机数→客户端验证书+生成pre-master→双方计算session key→对称加密通信。HTTPS的性能开销主要在握手阶段，HTTP/2和会话复用可大幅缓解。', '["网络","HTTP","HTTPS","TLS"]', 1),
('TCP的拥塞控制算法', '请介绍慢启动、拥塞避免、快速重传、快速恢复四个阶段。', '1.慢启动：连接建立后，拥塞窗口(cwnd)从1开始指数增长（每RTT翻倍），直到达到慢启动阈值(ssthresh)或发生丢包。2.拥塞避免：cwnd≥ssthresh后进入线性增长阶段（每RTT+1个MSS），避免突然拥塞。3.快速重传：接收方收到乱序包时立即发ACK（不等待延迟确认），发送方收到3个重复ACK认为丢包，立即重传而不等超时。4.快速恢复：快速重传后不移到慢启动（因为重复ACK说明还有数据在传输），将ssthresh设为当前cwnd/2，cwnd=ssthresh，进入拥塞避免阶段。这是TCP Reno算法的核心逻辑。', '["网络","TCP","拥塞控制"]', 3),
('DNS解析的完整过程', '请描述从输入URL到获取IP地址的完整DNS解析流程。', '1.浏览器缓存检查（Chrome可通过chrome://net-internals/#dns查看）。2.操作系统缓存检查（hosts文件优先于DNS缓存）。3.本地DNS服务器（由DHCP或手动配置，通常是路由器或运营商DNS）。4.如果本地DNS没有缓存：→根域名服务器（返回顶级域服务器的IP）→顶级域服务器（如.com返回权威DNS服务器IP）→权威DNS服务器（返回最终IP）。这是递归查询+迭代查询的组合。DNS基于UDP 53端口，区域传送用TCP。DNS优化：CDN的DNS调度（根据用户IP返回最近的CDN节点IP）、HTTPDNS（绕过运营商DNS防劫持）。', '["网络","DNS"]', 2),
('Cookie、Session和Token的区别及各自使用场景', '请从存储位置、安全性和跨域能力等角度对比。', 'Cookie：存储在浏览器端（4KB限制），每次请求自动带上（同域），可设置HttpOnly（防XSS）和Secure（仅HTTPS）。Session：存储在服务端（内存/Redis），客户端只存Session ID，安全性较高，但占用服务端资源，分布式需要共享存储。Token（如JWT）：服务端签发，客户端存储（localStorage/Cookie），请求时手动添加Header，服务端无需存储（无状态），支持跨域。JWT三个部分：Header（算法）、Payload（数据，base64编码非加密）、Signature（签名防篡改）。Token适用：前后端分离、移动端、分布式系统；Session适用：传统MVC、对实时失效要求高的场景。最佳实践：敏感信息存服务端Session，Token中只存用户标识，JWT配合Redis实现强制失效。', '["网络","Cookie","Session","JWT"]', 2),

-- ========== 操作系统 (2题) ==========
('进程和线程的区别', '请从资源分配、调度、通信、切换开销等角度对比。', '进程是资源分配的基本单位（独立内存地址空间、文件描述符表），线程是CPU调度的基本单位（同一进程内线程共享堆和方法区，各有独立程序计数器和栈）。切换开销：线程切换只需切换栈和寄存器（同一进程内），进程切换需要切换整个内存地址空间（页表切换），开销远大于线程。通信方式：进程间IPC（管道、消息队列、共享内存、Socket），线程直接读写共享变量（需同步）。创建开销：线程远小于进程（无需复制地址空间）。一个进程崩溃不影响其他进程，一个线程崩溃（如段错误）可能导致整个进程终止。', '["操作系统","进程","线程"]', 1),
('死锁的四个必要条件及预防方法', '请说明银行家算法和其他死锁处理策略。', '四个必要条件（缺一不可）：1.互斥条件——资源不能被多个进程共享。2.占有且等待——持有资源的同时等待其他资源。3.不可剥夺——已分配的资源不能被强制收回。4.循环等待——存在进程资源的环形等待链。处理策略：1.预防（破坏条件之一）：一次性分配所有资源（破坏占有且等待）、资源有序分配（破坏循环等待）。2.避免：银行家算法——每次分配前判断分配后是否仍处于安全状态（存在一种执行序列使所有进程完成）。3.检测与恢复：允许死锁发生，检测到后通过回滚事务或终止进程恢复。Java中：jstack可检测死锁线程，避免嵌套锁、统一加锁顺序可预防。', '["操作系统","死锁","银行家算法"]', 2),

-- ========== 设计模式及其他 (3题) ==========
('单例模式的几种实现方式', '请列举至少5种单例写法，并评价各自的优缺点。', '1.饿汉式：类加载时初始化，线程安全，可能浪费内存。2.懒汉式synchronized：调用时创建，线程安全但效率低。3.双重检查锁定(DCL)：volatile+两次null检查+synchronized块，线程安全且高效，推荐。4.静态内部类：利用类加载机制保证线程安全，延迟加载，推荐且简洁。5.枚举单例：Effective Java推荐，绝对安全（防反射和序列化破坏），但缺少灵活性。面试建议：DCL和静态内部类二选一，枚举适合明确需求的场景。Spring容器管理的Bean默认就是单例，业务开发中通常不需要手写单例。', '["设计模式","单例"]', 2),
('工厂模式和抽象工厂模式的区别', '请说明两种工厂模式的使用场景和区别。', '简单工厂：一个工厂类根据参数创建不同产品，不符合开闭原则（加新产品要改工厂代码）。工厂方法：每个产品对应一个工厂类，符合开闭原则，但类数量膨胀。抽象工厂：创建产品族（一组相关产品），如Spring的BeanFactory就是抽象工厂。工厂方法是一个工厂创建一个产品，抽象工厂是一个工厂创建一族产品。Spring中的应用：FactoryBean创建复杂Bean；ApplicationContext是抽象工厂，生产各种类型的Bean。', '["设计模式","工厂模式","Spring"]', 2),
('如何保证消息不被重复消费', '请从消息队列的角度分析幂等性方案。', '消息重复消费的原因：生产者重试（未收到ACK）、消费者已消费但ACK网络延迟。解决方案核心是幂等性。1.数据库唯一键约束：根据业务ID（订单号等）在数据库设唯一索引，重复插入直接失败。2.Redis原子操作：消费前SETNX（仅key不存在时set）记录消息ID为已处理，成功则处理否则跳过。3.前置状态判断：数据库update时带条件（UPDATE xx SET status=1 WHERE status=0 AND id=?），根据affected rows判断是否已处理。4.全局唯一消息ID：生产者带全局唯一ID，消费者用上述方案之一校验。实际项目中数据库唯一键最简单可靠，Redis方案适合高并发场景但需处理Redis不可用情况。', '["消息队列","幂等性","架构"]', 2);
